model User {
  id       String   @id @default(uuid())
  username String   @unique
  password String
}

datasource db {
  provider = "mysql"
  url      = env("DB_URL")
}

generator client {
  provider = "prisma-client-js"
}